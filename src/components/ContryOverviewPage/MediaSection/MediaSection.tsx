import React from 'react';

const MediaSection = () => {
  console.log('MediaSection rendered');

  return (
        <section>
            <p>MediaSection</p>
        </section>
  );
};

export default MediaSection;
